클래스 (객체): 필드나 함수(메소드)를 목적에 따라 묶어 놓은것
             묶음 객체
3요소 : 
        필드 (변수) 
                    값을 저장하는곳: 클래스 안에 작성합니다.
                    저장공간을 생성해주는것
                예)  int a;
        생성자함수 
                    객체를 만드는 함수: 
                    application 에서 사용하게 함
                    필드에 값을 저장하기도 함.
            override
            getter
            setter
            생성자 생성등
            
        메소드(함수)
                    어떤 기능을 작성하는 것: 클래스에서 작성
                예) void sum() {sout("합")}
                    int minus(int x, int y) {return x-y};

        객체만들기: 클래스 변수 = new  생성자 함수();
객체안에 필드/메ㅔ소드 사용법 : 변수.필드, 변수.메소드

    예)  Car car = new car ();
        c.sum();
        sout(c.name);

오버로딩: 함수명은 그대로 사용하게 하는 기능
        단 메게변수 개수, 자료형을 다르게 사용해야 합니다. 

    예) void sum(){}, void sum (int x){}등

상속 : 부모의 재산 (필드, 생성자, 메소드)을 마음대로 사용하는 기능
    예)public class SuperCar extends Car{} 
 사용법: public class 자식      extends  부모{ }

추상클래스 : 추상메소드 앞에 abstract
    예)  public abstact calss Car{}

추상메소드 : { } 가 없는 메소드 앞에 abstract 붙이면 됨


인터페이스 : 모든 메소드(함수)가 추상메소드가 된다
    앞에 interface 를 붙이면 됨
    메소드 앞에 abstract를 생략해도 됨
    사용법 : public interface 이터페이스명{}
        예) public interface Remote {void Sum();

추상크래스 (1개라도 추상 메소드가 있으면 됨)
                VS
인테페이스 (모든 메소드가 추상 메소드여야함)질문

다형성 : 함수 코딩은 동일한테 결과과 다양하게 나오는 기능을 말함
    예) Remote r=new Tv();
        부모 변수 = new 자식생성자();



# 기술 면접 질문: 1) 추상클래스(1개라도 추상메소드가 있으면 됨) vs 인터페이스(모든 메소드가 추상메소드이어야함) 질문?
                2) 객체지향 언어의 특징에 대해 설명해 보세요
                3) 자바 특징에 대해 설명해 보세요

# 객체지향 언어: 대표적으로 자바가 있구요, 객체를 만들어서 코딩하는 것을 말합니다.
# 객체지향 특징: 1) 상속 2) 오버로딩 3) 오버라이딩 4) 다형성
# 자바: 객체지향 언어, 어떤 OS(운영체제)에서도 실행되는 좋은 언어입니다. 내부적으로 JVM(자바가상머신)이 있습니다.
